# Ensure binder_linux creates all three device nodes needed by Waydroid.
# Required on some kernel builds where the defaults do not include all three.
options binder_linux devices=binder,hwbinder,vndbinder
