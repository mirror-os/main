# Load the Android binder kernel modules required by Waydroid.
# binder_linux provides /dev/binder, /dev/hwbinder, and /dev/vndbinder
# which the LXC container needs to communicate with Android services.
binder_linux
